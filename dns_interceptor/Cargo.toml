[package]
name = "dns_interceptor"
version = "0.1.0"
edition = "2024"

[dependencies]
base64 = "0.23.1"
reqwest = { version = "0.13.5", default-features = false, features = ["blocking", "json", "query", "rustls"] }
serde = { version = "1.0.229", features = ["derive"] }
